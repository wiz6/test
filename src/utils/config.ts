export const PORT: number = Number(process.env.API_PORT) || 3000;

export const PIPEDRIVE_API: string = process.env.PIPEDRIVE_API;

export const PIPEDRIVE_API_TOKEN: string = process.env.PIPEDRIVE_API;
